import React from "react";

// i18n
import { FormattedMessage } from "react-intl";

export default props => (
  <p>
    <FormattedMessage id="common.home" description="Título da página inicial" />
  </p>
);
