import React from "react";
import Card from "../../../components/ui/Card";
import CardBody from "../../../components/ui/CardBody";

export default (props) => (
  <Card>
    <CardBody>
      Nenhuma configuração a respeito das PRIVACIDADE disponível.
    </CardBody>
  </Card>
);
